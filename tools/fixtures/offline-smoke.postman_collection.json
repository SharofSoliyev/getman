{
  "info": {
    "name": "GetMan CLI smoke test",
    "description": "Runs without a network. The one request points at a closed local port, so the connection is refused immediately and the run must end with exit code 1 and a JUnit report that records the failure.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    { "key": "closedPort", "value": "http://127.0.0.1:1" }
  ],
  "item": [
    {
      "name": "Unreachable host",
      "request": {
        "method": "GET",
        "header": [],
        "url": { "raw": "{{closedPort}}/never" }
      }
    }
  ]
}
